package router

import (
	"github"
	"github.com/gorilla/mux"
)

func Router() {
	router := mux.NewRouter()

	router.HandleFunc("/book{id}", )
	router.HandleFunc()
	router.HandleFunc()
	router.HandleFunc()
	router.HandleFunc()

	return
}
